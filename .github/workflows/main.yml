name: 24/7 Live Stream Bot

on:
  workflow_dispatch:
    inputs:
      target_url:
        description: 'Target Stream URL'
        required: true
        type: string
        default: 'https://dadocric.st/player.php?id=willowextra'
      okru_stream_channel: # 🚀 NAYA: OK.ru Stream Channel Selector
        description: 'OK.ru Broadcast Channel (1 to 5)'
        required: true
        type: choice
        options:
          - '1'
          - '2'
          - '3'
          - '4'
          - '5'
        default: '1'
      use_proxy:
        description: 'Use Proxy (ON or OFF)'
        required: true
        type: choice
        options:
          - 'OFF'
          - 'ON'
        default: 'OFF'
      stream_quality:
        description: 'Internet Speed / Stream Quality'
        required: true
        type: choice
        options:
          - '40KBps (Ultra-Low 360p)'
          - '110KBps (Balanced 480p)'
        default: '110KBps (Balanced 480p)'
  # schedule:
  #   - cron: '0 */5 * * *'
# Runs at 0 minutes past the hour. every 5 hours (matlb har 5 hours k baaad yeh khud ba khud run ho jata hai yeh actin)

permissions:
  contents: write

jobs:
  stream:
    runs-on: ubuntu-latest

    steps:
    - name: Get Code
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install FFmpeg, Virtual Display, & Audio Server
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg xvfb pulseaudio dbus-x11

    - name: Install Required Packages
      run: npm install puppeteer puppeteer-extra puppeteer-extra-plugin-stealth puppeteer-screen-recorder

    - name: Start Live Stream
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        TARGET_URL: ${{ github.event.inputs.target_url || 'https://dadocric.st/player.php?id=starsp3&v=m' }}
        OKRU_STREAM_ID: ${{ github.event.inputs.okru_stream_channel || '1' }} # 🚀 NAYA: Channel ID Node.js ko bhej rahe hain
        USE_PROXY: ${{ github.event.inputs.use_proxy || 'OFF' }}
        STREAM_QUALITY: ${{ github.event.inputs.stream_quality || '110KBps (Balanced 480p)' }}
      run: |
        # Start headless audio server for FFmpeg to capture
        pulseaudio -D --exit-idle-time=-1
        pacmd load-module module-virtual-sink sink_name=v1
        pacmd set-default-sink v1
        pacmd set-default-source v1.monitor

        # Run Chromium inside a Virtual Monitor with exact 1280x720 HD resolution
        xvfb-run -s "-screen 0 1280x720x24 -ac" node browser.js










































































































# 0 ============= Done , bass aab multi stream add karney hai ok.ru ======================= 




# name: 24/7 Live Stream Bot

# on:
#   workflow_dispatch:
#     inputs:
#       target_url: # 🚀 NAYA: URL Input Box
#         description: 'Target Stream URL'
#         required: true
#         type: string
#         default: 'https://dadocric.st/player.php?id=starsp3&v=m'
#       use_proxy:
#         description: 'Use Proxy (ON or OFF)'
#         required: true
#         type: choice
#         options:
#           - 'OFF'
#           - 'ON'
#         default: 'OFF'
#       stream_quality:
#         description: 'Internet Speed / Stream Quality'
#         required: true
#         type: choice
#         options:
#           - '40KBps (Ultra-Low 360p)'
#           - '110KBps (Balanced 480p)'
#         default: '110KBps (Balanced 480p)'
#   schedule:
#     - cron: '0 */5 * * *'

# permissions:
#   contents: write

# jobs:
#   stream:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Get Code
#       uses: actions/checkout@v3

#     - name: Setup Node.js
#       uses: actions/setup-node@v3
#       with:
#         node-version: '18'

#     - name: Install FFmpeg, Virtual Display, & Audio Server
#       run: |
#         sudo apt-get update
#         sudo apt-get install -y ffmpeg xvfb pulseaudio dbus-x11

#     - name: Install Required Packages
#       run: npm install puppeteer puppeteer-extra puppeteer-extra-plugin-stealth puppeteer-screen-recorder

#     - name: Start Live Stream
#       env:
#         GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         TARGET_URL: ${{ github.event.inputs.target_url || 'https://dadocric.st/player.php?id=starsp3&v=m' }} # 🚀 NAYA: URL pass ho raha hai
#         USE_PROXY: ${{ github.event.inputs.use_proxy || 'OFF' }}
#         STREAM_QUALITY: ${{ github.event.inputs.stream_quality || '110KBps (Balanced 480p)' }}
#       run: |
#         # Start headless audio server for FFmpeg to capture
#         pulseaudio -D --exit-idle-time=-1
#         pacmd load-module module-virtual-sink sink_name=v1
#         pacmd set-default-sink v1
#         pacmd set-default-source v1.monitor

#         # Run Chromium inside a Virtual Monitor with exact 1280x720 HD resolution
#         xvfb-run -s "-screen 0 1280x720x24 -ac" node browser.js


        














































































































# ================ great bas aab oopper waley code mei eek new manually input add karna hai taakey mei manually url dee sko ============================





# name: 24/7 Live Stream Bot

# on:
#   workflow_dispatch:
#     inputs:
#       use_proxy:
#         description: 'Use Proxy (ON or OFF)'
#         required: true
#         type: choice
#         options:
#           - 'OFF'
#           - 'ON'
#         default: 'OFF'
#       stream_quality: # 🚀 NAYA: Quality select karne ka dropdown
#         description: 'Internet Speed / Stream Quality'
#         required: true
#         type: choice
#         options:
#           - '40KBps (Ultra-Low 360p)'
#           - '110KBps (Balanced 480p)'
#         default: '110KBps (Balanced 480p)'
#   schedule:
#     - cron: '0 */5 * * *'

# permissions:
#   contents: write

# jobs:
#   stream:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Get Code
#       uses: actions/checkout@v3

#     - name: Setup Node.js
#       uses: actions/setup-node@v3
#       with:
#         node-version: '18'

#     - name: Install FFmpeg, Virtual Display, & Audio Server
#       run: |
#         sudo apt-get update
#         sudo apt-get install -y ffmpeg xvfb pulseaudio dbus-x11

#     - name: Install Required Packages
#       run: npm install puppeteer puppeteer-extra puppeteer-extra-plugin-stealth puppeteer-screen-recorder

#     - name: Start Live Stream
#       env:
#         GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         USE_PROXY: ${{ github.event.inputs.use_proxy || 'OFF' }}
#         STREAM_QUALITY: ${{ github.event.inputs.stream_quality || '110KBps (Balanced 480p)' }} # 🚀 NAYA: Node.js ko input bhej rahe hain
#       run: |
#         # Start headless audio server for FFmpeg to capture
#         pulseaudio -D --exit-idle-time=-1
#         pacmd load-module module-virtual-sink sink_name=v1
#         pacmd set-default-sink v1
#         pacmd set-default-source v1.monitor

#         # Run Chromium inside a Virtual Monitor with exact 1280x720 HD resolution
#         xvfb-run -s "-screen 0 1280x720x24 -ac" node browser.js
















































































































# // ============= done 110kbps internet speed k liye smooth streaming =====================



# name: 24/7 Live Stream Bot

# on:
#   workflow_dispatch:
#     inputs:
#       use_proxy:
#         description: 'Use Proxy (ON or OFF)'
#         required: true
#         type: choice
#         options:
#           - 'OFF'
#           - 'ON'
#         default: 'OFF'
#   schedule:
#     - cron: '0 */5 * * *'

# # NAYA: Releases create karne ki ijazat deni hogi GitHub Actions ko
# permissions:
#   contents: write

# jobs:
#   stream:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Get Code
#       uses: actions/checkout@v3

#     - name: Setup Node.js
#       uses: actions/setup-node@v3
#       with:
#         node-version: '18'

#     - name: Install FFmpeg, Virtual Display, & Audio Server
#       run: |
#         sudo apt-get update
#         sudo apt-get install -y ffmpeg xvfb pulseaudio dbus-x11

#     - name: Install Required Packages
#       # NAYA: puppeteer-screen-recorder install add kiya gaya hai
#       run: npm install puppeteer puppeteer-extra puppeteer-extra-plugin-stealth puppeteer-screen-recorder

#     - name: Start Live Stream
#       env:
#         GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         USE_PROXY: ${{ github.event.inputs.use_proxy || 'OFF' }}
#       run: |
#         # Start headless audio server for FFmpeg to capture
#         pulseaudio -D --exit-idle-time=-1
#         pacmd load-module module-virtual-sink sink_name=v1
#         pacmd set-default-sink v1
#         pacmd set-default-source v1.monitor

#         # Run Chromium inside a Virtual Monitor with exact 1280x720 HD resolution
#         xvfb-run -s "-screen 0 1280x720x24 -ac" node browser.js















# ============= done fullscreen capture and broadscast to ok.ru =====================



# name: 24/7 Live Stream Bot

# on:
#   workflow_dispatch:
#     inputs:
#       use_proxy:
#         description: 'Use Proxy (ON or OFF)'
#         required: true
#         type: choice
#         options:
#           - 'OFF'
#           - 'ON'
#         default: 'OFF'
#   schedule:
#     - cron: '0 */5 * * *'

# # NAYA: Releases create karne ki ijazat deni hogi GitHub Actions ko
# permissions:
#   contents: write

# jobs:
#   stream:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Get Code
#       uses: actions/checkout@v3

#     - name: Setup Node.js
#       uses: actions/setup-node@v3
#       with:
#         node-version: '18'

#     - name: Install FFmpeg, Virtual Display, & Audio Server
#       run: |
#         sudo apt-get update
#         sudo apt-get install -y ffmpeg xvfb pulseaudio dbus-x11

#     - name: Install Required Packages
#       # NAYA: puppeteer-screen-recorder install add kiya gaya hai
#       run: npm install puppeteer puppeteer-extra puppeteer-extra-plugin-stealth puppeteer-screen-recorder

#     - name: Start Live Stream
#       env:
#         GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         USE_PROXY: ${{ github.event.inputs.use_proxy || 'OFF' }}
#       run: |
#         # Start headless audio server for FFmpeg to capture
#         pulseaudio -D --exit-idle-time=-1
#         pacmd load-module module-virtual-sink sink_name=v1
#         pacmd set-default-sink v1
#         pacmd set-default-source v1.monitor

#         # Run Chromium inside a Virtual Monitor with exact 1280x720 HD resolution
#         xvfb-run -s "-screen 0 1280x720x24 -ac" node browser.js

















































































# // ===================== I think proxy nahey hai is liyee video nahy play hu raha hai =========================



# name: 24/7 Live Stream Bot

# on:
#   workflow_dispatch: # Aapko manual start/stop ka button dega
#   schedule:
#     - cron: '0 */5 * * *' # Har 5 ghante baad server refresh karega (No downtime)

# jobs:
#   stream:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Get Code
#       uses: actions/checkout@v3

#     - name: Setup Node.js
#       uses: actions/setup-node@v3
#       with:
#         node-version: '18'

#     - name: Install FFmpeg & Virtual Display (Xvfb)
#       run: |
#         sudo apt-get update
#         sudo apt-get install -y ffmpeg xvfb

#     - name: Install Required Packages
#       run: npm install puppeteer puppeteer-extra puppeteer-extra-plugin-stealth

#     - name: Start Live Stream
#       # xvfb-run server par ek nakli screen banata hai taake Puppeteer headless:false chal sake
#       run: xvfb-run --auto-servernum node browser.js
